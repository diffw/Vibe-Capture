# Testing Toolchain: UI — Swift (macOS)

> Auto-generated by .cursor/skills/testing/00-toolchain-generator/SKILL.md  
> Project: VibeCap (VibeCapture.xcodeproj)  
> Tech Stack: Swift + SwiftUI (macOS 14.0+)  
> Generated: 2026-03-11  
> Regenerate this file if the tech stack changes significantly.

---

## Linked Methodology Skill

This file provides platform-specific toolchain config for:  
→ `.cursor/skills/testing/04-ui-test/SKILL.md`

---

## Test Framework & Setup

- Framework: XCUITest
- Required dependencies: Built-in XCUITest
- Config file: Xcode scheme `VibeCap` with UI test target `VibeCapUITests`
- Test file location: `VibeCapture/Tests/VibeCaptureUITests/`
- File naming: `*UITests.swift`
- Run command: `xcodebuild -scheme VibeCap -destination 'platform=macOS' test -only-testing:VibeCapUITests`

---

## Code Patterns

### Basic Test Structure
```swift
import XCTest

final class PaywallUITests: XCTestCase {
    private let app = XCUIApplication()

    override func setUpWithError() throws {
        continueAfterFailure = false
        app.launch()
    }

    func testPaywallDisplaysPlans() {
        app.buttons["upgrade-button"].tap()
        XCTAssertTrue(app.staticTexts["plan-title"].waitForExistence(timeout: 2))
    }
}
```

### Mock / Stub Patterns
- Pass launch arguments for deterministic data: `app.launchArguments += ["-useMockPurchaseData", "1"]`.
- Use `app.launchEnvironment` to fix locale for consistent assertions.

### Setup & Teardown
- Disable animations if flakiness appears: `app.launchArguments += ["-ApplePersistenceIgnoreState", "YES"]`.

---

## Platform-Specific Considerations

- macOS UI tests run on the host Mac (no simulator). Windows may need explicit activation: `app.activate()`.
- Add accessibility identifiers to SwiftUI views: `.accessibilityIdentifier("plan-title")`.
- macOS supports hover, right-click, and keyboard navigation — test these where relevant.
- For menu bar app testing, use `XCUIApplication(bundleIdentifier:)` if needed.
- Keep flows short; prefer behavior assertions (visibility, text, toggles) over layout pixel checks.
