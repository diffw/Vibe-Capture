# Testing Toolchain: Performance — Swift (macOS)

> Auto-generated by .cursor/skills/testing/00-toolchain-generator/SKILL.md  
> Project: VibeCap (VibeCapture.xcodeproj)  
> Tech Stack: Swift + SwiftUI (macOS 14.0+)  
> Generated: 2026-03-11  
> Regenerate this file if the tech stack changes significantly.

---

## Linked Methodology Skill

This file provides platform-specific toolchain config for:  
→ `.cursor/skills/testing/10-performance-test/SKILL.md`

---

## Test Framework & Setup

- Framework: XCTest Performance
- Required dependencies: Built-in XCTest
- Config file: Use `measure` blocks in `VibeCapTests`
- Test file location: `VibeCapture/Tests/VibeCaptureTests/Performance/`
- File naming: `*PerformanceTests.swift`
- Run command: `xcodebuild -scheme VibeCap -destination 'platform=macOS' test -only-testing:VibeCapTests/Performance`

---

## Code Patterns

### Basic Test Structure
```swift
import XCTest
@testable import VibeCap

final class ImageProcessingPerformanceTests: XCTestCase {
    func testCropPerformance() throws {
        let testImage = NSImage(size: NSSize(width: 3840, height: 2160))
        let cropRect = CGRect(x: 100, y: 100, width: 1920, height: 1080)

        measure(metrics: [XCTClockMetric(), XCTCPUMetric(), XCTMemoryMetric()]) {
            _ = ImageProcessor.crop(testImage, to: cropRect)
        }
    }
}
```

### Mock / Stub Patterns
- Use in-memory fixtures; avoid disk IO.
- Ensure caches are warmed before `measure` block.

### Setup & Teardown
- Run on Release build settings for accurate numbers; disable debug logging.
- Establish baseline in Xcode; allow ≤ 10% regression threshold.

---

## Platform-Specific Considerations

- Focus on hot paths: screen capture processing, image cropping, clipboard operations, OCR (if applicable).
- macOS has more memory and CPU headroom than iOS — set appropriate thresholds.
- Keep measurement blocks minimal; avoid UI or network inside performance tests.
- Use `XCTMemoryMetric()` to catch memory leaks in image processing pipelines.
